# FLUX Workshop — Example Programs
#
# Each .asm file is FLUX assembly source.
# Use the CLI to assemble and run:
#
#   python cli.py assemble workshop/addition.asm -o /tmp/addition.flux
#   python cli.py run /tmp/addition.flux
#   python cli.py disassemble /tmp/addition.flux

See the individual .asm files in this directory for examples.
